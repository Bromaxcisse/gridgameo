import 'dart:ui';

abstract final class AppColors {
  // Backgrounds & Structure
  static const Color deepVoid = Color(0xFF05050A);
  static const Color terminalDim = Color(0xFF1A1A2E);
  static const Color panelBackground = Color(0xE60F0F1A); // 90% opacity

  // Primary Accents (Neon Glows)
  static const Color cyanPlasma = Color(0xFF00F0FF);
  static const Color neonIsotope = Color(0xFF39FF14);
  static const Color coreBreach = Color(0xFFFF003C);
  static const Color amberAlert = Color(0xFFFFD700);

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF8892B0);
}
